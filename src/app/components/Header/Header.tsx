import AppBar from '@mui/material/AppBar';

import { NavBarItem } from '../../types';

const NavBarChoices: NavBarItem[] = [
  {
    name: 'Welcome',
    route: ''
  }
]

const Header = (): JSX.Element => {
  return (
    <AppBar>
    
    </AppBar>
  );
}

export default Header
